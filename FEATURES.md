# FastForm 填写记忆：实用 / 炫技

油猴脚本 `youhou.js`。挂在内网页上，记住表单、一键回填、空字段虚拟数据。

生效地址：`192.168.120.228`、`192.168.100.156`。两个 IP 按 **路径** 共用历史，不跟域名走。

---

## 实用

每天真会用到的能力。

### 操作

| 动作 | 入口 | 说明 |
| --- | --- | --- |
| 保存 | 菜单「保存」 / `Alt+S` | 记下当前页所有识别到的表单。可写备注。每页最多 30 条。 |
| 回填 | 菜单「回填」 / `Alt+F` | 直接写入最新一条，不弹勾选。 |
| 勾选回填 | `Shift`+点「回填」 / `Alt+Shift+F` / 历史里的「勾选」 | 先勾字段再写。 |
| 虚拟 | 菜单「虚拟」 / `Alt+V` | 只给**空字段**造测试数据，先预览再点「写入」或按 Enter。 |
| 历史 | 菜单「历史」 | 本页档案：按备注 / 字段名 / 字段值搜索，回填、勾选、复制 JSON、改备注、删除、清空本页。Esc 先清空搜索再关面板。 |
| 身份 | 菜单「身份」 / `Alt+U` | 测试账号档，不跟页面历史混。登录页填好账号密码后点「保存」，会问项目分组。面板顶部分组筛选；行内可改项目。同一账号可分属不同项目。切登录、验证码规则不变。密码只存在油猴本地。最多 36 个。 |
| 重置 | 菜单「重置」 / `Alt+R` | 清本站 localStorage / sessionStorage / Cookie / Cache Storage 并重置 token，然后刷新。填核档案和身份档保留。HttpOnly Cookie 清不掉。 |
| 抓包 | 菜单「抓包」 / `Alt+N` | 记下本页最近 40 条接口（fetch / XHR）。点行展开请求/响应；复制 JSON、cURL；「写入」把请求体填进当前表，「响应」把返回 data 填进表。静态资源和心跳会跳过。只存在内存，刷新即丢。 |
| 复制 / 导入 | 菜单「复制」「导入」 | 剪贴板 JSON，方便换机器或改完再导回来。 |
| 单字段 | 字段上右键 | 「虚拟此字段 / 回填此字段」。 |

悬浮球：单击开菜单，拖动换位置，双击复位到右下角。预览打开时菜单会收起，避免挡住「写入」。

### 写入规则

- 引擎顺序：FastForm `getValues/setValues` → Element Plus / Vant 表单 → 原生 input。
- **有弹窗时只写弹窗里的表**，不污染外面的查询表。
- 虚拟预览可全选 / 反选；Enter 确认，Esc 关闭。
- 跳过：密码、验证码、上传、Element Plus 内部 id（`el-id-*`）。身份档切登录除外。
- 虚拟按字段名/标签猜类型：手机、姓名、邮箱、身份证、日期时间、区间、备注、地址、金额、下拉第一项等。
- 非空字段虚拟时跳过，避免覆盖已填内容。
- 历史按 `pathname + hash` 存，油猴 `GM_*` 为主，顺带写 `localStorage`。
- 身份档全局共用，按**项目分组**，不跟页面走。未填分组的归入「未分组」。

### 场景

1. 联调弹窗：填一遍 → 保存 → 下次回填。
2. 空表冒烟：打开弹窗 → 虚拟 → 核对预览 → 写入。
3. 只要某一列：右键该字段，或历史里勾选。
4. 换环境：复制 JSON，到另一台导入。
5. 切登录：登录页保存身份，之后菜单「身份」点「切换」。
6. Token 脏了 / 字典缓存不对：菜单「重置」，确认后刷新。
7. 对接口：打开「抓包」，提交后点那条复制 cURL 或把请求体写回表。

---

## 炫技

不影响填表对错，去掉也能用。全是视觉。

### 悬浮球

- 六边形按钮，双圈呼吸（关）→ 机甲面甲 + 触角（开）。
- SVG path 插值变形，不是切图。
- 虚线六边环慢转；打开时上方光柱再展开菜单。

### 菜单

- 全息：透视折叠、扫描条、顶边流光、偶发闪烁。
- `CMD GRID`、编号 01–09、中英标签、切角按钮、hover 扫光。
- 打开预览时瞬间藏菜单（`dock`），避免动画还挡着按钮。

### 面板

- 青黑底、网格、切角、四角括号、历史列表扫光。
- 虚拟预览贴在球左侧，底栏钉住，「写入」是绿色主按钮。

### 写入反馈

- 底部 toast 报成功 / 跳过 / 失败数。

---

## 快捷键

| 键 | 作用 |
| --- | --- |
| `Alt+S` | 保存 |
| `Alt+F` | 回填最新一条 |
| `Alt+Shift+F` | 勾选后回填 |
| `Alt+V` | 虚拟预览 |
| `Alt+U` | 身份档 |
| `Alt+R` | 清空缓存并重置 token |
| `Alt+N` | 请求抓包 |
| `Enter` | 预览里确认写入 |
| `Esc` | 关预览 / 历史（历史有搜索词时先清空） |

---

## 还能加什么（当时列过的）

当时问「还能加什么功能」时列的清单。已做的标了，没做的仍可做。

### 填表

| 功能 | 状态 |
| --- | --- |
| 字段勾选再回填 / 虚拟 | 已做。回填默认直接写；`Shift`+回填或历史「勾选」才弹面板。 |
| 虚拟写入前预览 | 已做 |
| 快捷键 `Alt+S/F/V/U/R` | 已做 |
| 右键输入框：只虚拟 / 回填这一格 | 已做 |
| 填完计数：成功 / 跳过 / 失败 | 已做 |
| 弹窗、抽屉里的表 | 已做：有弹窗时只写弹窗 |
| 只填空 / 全量覆盖，虚拟和回填都能选 | 未做。现在虚拟只填空，回填会覆盖 |
| 必填空项标出来，点一下跳过去 | 未做 |
| 下拉、级联、日期：选项树拿不到就填不了，要补强 | 未做。现在有 options 才选第一项 |
| 固定身份档：常用联系人、测试账号，不跟页面历史混 | 已做测试账号切登录，并按项目分组。联系人填表仍未做。 |
| 动态显隐字段再填一轮 | 已有延迟二次写入，复杂联动仍可能漏 |

### 历史和复用

| 功能 | 状态 |
| --- | --- |
| 跨 IP 同一路径共用历史 | 已做（油猴 `GM_*`） |
| 剪贴板复制 / 导入 JSON | 已做 |
| 跨页带字段：路径不同但「手机、姓名」能复用 | 未做 |
| 按备注、字段值搜历史 | 已做。历史面板顶部搜索，匹配备注、字段名、字段值；预览优先显示命中字段。 |
| 收藏置顶 | 未做 |
| 导出 / 导入 JSON **文件**，换电脑接着用 | 未做 |
| 对比两条历史，看改了哪些字段 | 未做 |
| 一键清空本页历史 | 已做。历史面板标题栏「清空」，会确认。 |
| 敏感字段预览打码 | 未做 |

### 虚拟数据

| 功能 | 状态 |
| --- | --- |
| 手机、姓名、邮箱、备注、日期、金额、地址、公司、身份证 | 已做 |
| 规则可改：号段、姓名池、邮箱域名 | 未做 |
| 每次随机 / 固定种子（联调要稳，演示要乱） | 未做 |
| 银行卡按 Luhn、统一社会信用代码按校验位 | 未做 |
| 预览里改值、再随机一次再写入 | 未做 |

### 探测和危险项

| 功能 | 状态 |
| --- | --- |
| iframe 内表单 | 未做 |
| 请求抓包（fetch / XHR） | 已做。菜单「抓包」；只记内存，刷新即丢。不点提交。 |
| 填完自动点「查询 / 提交」 | 未做。必须开关，默认关 |
| 密码、验证码、真上传 | 虚拟/普通回填仍跳过。身份档可存测试账号密码并切登录，只存在油猴本地；有验证码不自动点登录。 |

---

## 炫技（当时列过的）

和 HUD 搭，但不影响填对填错。字段扫光做过，会扫到弹窗外，**已经去掉**。

**还没做、可以做**

- 瞄准镜：鼠标移到输入框，六边形锁住，显示识别类型（手机 / 姓名）
- 开机自检：进页 `SCAN FORM… FastForm ×1 READY`
- 失败闪红、成功闪青，钉在控件上（比整页扫光小）
- 迷你雷达：球上小点表示本页空字段数
- 引擎换色：FastForm 青、Element 蓝、Vant 绿
- 省动效开关
- 演示：打字机一个字一个字填（默认关）

**偏玩、不优先**

- 指挥模式：按住 Alt 给字段编号再按数字写
- 手势：球上画圈=虚拟，向右甩=回填
- 侧边终端日志 `WRITE phone=138… OK`
- 手机号滚轮、身份证 checksum 闪一下
- 表单节点图、3D 翻转预览
- 语音口令、成就计数、伪装 `ff> fill --latest`

当时说如果做炫技，性价比高的是：**瞄准识别框 + 开机 SCAN**（扫光已弃）。

后来补过、原清单里没有的：撤销上一次写入、失败只重试、按校验规则造数、可编辑表格先点新增再填行、分步向导、弹窗打开自动填、`@match` 可配、请求抓包。

---

## 维护时怎么拆

改填表逻辑看：`currentForms`、`applyMerge`、`planVirtual`、`preferOverlay`、存储 key。

改皮看：`#ff-hud` CSS、`GLYPH` / `morphGlyph`、菜单 `clip-path` 动画。
