# FastForm 填写记忆：实用 / 炫技

油猴脚本 `youhou.js`。挂在内网页上，记住表单、一键回填、空字段虚拟数据。

生效地址：`192.168.120.228`、`192.168.100.156`。两个 IP 按 **路径** 共用历史，不跟域名走。

---

## 实用

每天真会用到的能力。

### 操作

| 动作 | 入口 | 说明 |
| --- | --- | --- |
| 保存 | 菜单「保存」 / `Alt+S` | 记下当前页所有识别到的表单。可写备注。每页最多 30 条。 |
| 回填 | 菜单「回填」 / `Alt+F` | 直接写入最新一条，不弹勾选。 |
| 勾选回填 | `Shift`+点「回填」 / `Alt+Shift+F` / 历史里的「勾选」 | 先勾字段再写。 |
| 虚拟 | 菜单「虚拟」 / `Alt+V` | 只给**空字段**造测试数据，先预览再点「写入」或按 Enter。 |
| 历史 | 菜单「历史」 | 本页档案：回填、勾选、复制 JSON、改备注、删除。 |
| 复制 / 导入 | 菜单「复制」「导入」 | 剪贴板 JSON，方便换机器或改完再导回来。 |
| 单字段 | 字段上右键 | 「虚拟此字段 / 回填此字段」。 |

悬浮球：单击开菜单，拖动换位置，双击复位到右下角。预览打开时菜单会收起，避免挡住「写入」。

### 写入规则

- 引擎顺序：FastForm `getValues/setValues` → Element Plus / Vant 表单 → 原生 input。
- **有弹窗时只写弹窗里的表**，不污染外面的查询表。
- 虚拟预览可全选 / 反选；Enter 确认，Esc 关闭。
- 跳过：密码、验证码、上传、Element Plus 内部 id（`el-id-*`）。
- 虚拟按字段名/标签猜类型：手机、姓名、邮箱、身份证、日期时间、区间、备注、地址、金额、下拉第一项等。
- 非空字段虚拟时跳过，避免覆盖已填内容。
- 历史按 `pathname + hash` 存，油猴 `GM_*` 为主，顺带写 `localStorage`。

### 场景

1. 联调弹窗：填一遍 → 保存 → 下次回填。
2. 空表冒烟：打开弹窗 → 虚拟 → 核对预览 → 写入。
3. 只要某一列：右键该字段，或历史里勾选。
4. 换环境：复制 JSON，到另一台导入。

---

## 炫技

不影响填表对错，去掉也能用。全是视觉。

### 悬浮球

- 六边形按钮，双圈呼吸（关）→ 机甲面甲 + 触角（开）。
- SVG path 插值变形，不是切图。
- 虚线六边环慢转；打开时上方光柱再展开菜单。

### 菜单

- 全息：透视折叠、扫描条、顶边流光、偶发闪烁。
- `CMD GRID`、编号 01–06、中英标签、切角按钮、hover 扫光。
- 打开预览时瞬间藏菜单（`dock`），避免动画还挡着按钮。

### 面板

- 青黑底、网格、切角、四角括号、历史列表扫光。
- 虚拟预览贴在球左侧，底栏钉住，「写入」是绿色主按钮。

### 写入反馈

- 底部 toast 报成功 / 跳过 / 失败数。

---

## 快捷键

| 键 | 作用 |
| --- | --- |
| `Alt+S` | 保存 |
| `Alt+F` | 回填最新一条 |
| `Alt+Shift+F` | 勾选后回填 |
| `Alt+V` | 虚拟预览 |
| `Enter` | 预览里确认写入 |
| `Esc` | 关预览 / 历史 |

---

## 未来可做

还没做。按值不按炫，前面的更值得先做。

### 实用

**填表**

- 预览里直接改值再写入，不用先导入 JSON。
- 虚拟「再随机一次」，同一预览刷新手机号 / 姓名。
- 回填也支持「只填空 / 覆盖」，现在回填会盖掉已填。
- 保存只存当前弹窗，不要把查询表一起记进去。
- 钉死一条默认档案，回填永远用它，不跟最新一条走。
- 撤销上一次写入。
- 失败字段列表，点一下只重试失败的。
- 按校验规则造数：必填、正则、最大长度、日期上下限（FastForm / Element 的 rules）。
- 下拉按选项文案选，不要永远第一项；级联走完整路径。
- 可编辑表格：先点「新增」再填行。现在只认表单字段。
- 向导 / 分步：填完第一步再填下一步。
- 字段改名后用标签对齐旧档案（`承建联系人` 对得上 `concatUser`）。

**场景**

- 弹窗一打开就自动虚拟或回填（可关）。
- 写入后自动点「确定 / 提交」（必须开关，默认关）。
- 跨页模板：登录、新建项目这种不跟 pathname 绑死。
- 整库导出 / 导入所有页面历史，不限当前页。
- 油猴菜单开关：本页禁用、只填空。
- `@match` 可配，不写死两个内网 IP。
- iframe、富文本、远程下拉（输入关键字才有选项）目前基本填不了。

**别做的**

- 自动填验证码、密码、真上传文件。容易出事，继续跳过。

### 炫技

- 省动效：系统「减少动态效果」或菜单一键关掉扫描 / 变形。
- 识别到 FastForm / ElForm 时球上亮一颗引擎灯。
- 第二套配色（琥珀 / 绿灯），跟站点皮肤对上。
- 演示模式：字段一个一个慢填，方便录屏。不作为日常默认。

---

## 维护时怎么拆

改填表逻辑看：`currentForms`、`applyMerge`、`planVirtual`、`preferOverlay`、存储 key。

改皮看：`#ff-hud` CSS、`GLYPH` / `morphGlyph`、菜单 `clip-path` 动画。
